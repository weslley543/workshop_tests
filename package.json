{
  "name": "training_exercises",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "tslint -c ./tslint.json -p tsconfig.json --fix",
    "test": "jest --collect-coverage",
    "test:watch": "jest --watchAll",
    "test:watchAll": "jest --collect-coverage --watchAll"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "ts-jest": "^27.0.2",
    "tslint": "^6.1.3",
    "typescript": "^4.3.2",
    "jest": "^27.0.4"
  }
}
